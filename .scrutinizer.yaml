build:
  environment:
    image: default-focal   # Ubuntu 20.04 with OpenSSL 1.1.1
    php: 8.1

  nodes:
    analysis:
      tests:
        override:
          - php-scrutinizer-run
          - js-scrutinizer-run
